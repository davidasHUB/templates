# TSI 2025

projeto Django feito no IF

Para rodar esse projeto é recomendado um venv com django é django-widget-tweaks

```
python -m venv venv
venv\Scripts\activate

pip install django==6.0.3
pip install django-widget-tweaks==1.5.1
```